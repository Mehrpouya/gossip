import { GossipHashtagPipe } from './gossip-hashtag.pipe';

describe('GossipHashtagPipe', () => {
  it('create an instance', () => {
    const pipe = new GossipHashtagPipe();
    expect(pipe).toBeTruthy();
  });
});
